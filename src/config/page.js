// Printed page dimensions: US Letter at 96 dpi (8.5in × 11in). The PDF
// export places the page capture at exactly 8.5×11in, so every scale and
// fit computation must use these values.
// Keep in sync with --page-w / --page-h in the stylesheet.
export const PAGE_W = 816
export const PAGE_H = 1056
